import LoginForm from "../components/LoginForm"

function LoginPage() {
  return (
    <div className="flex items-center justify-center bg-primary" style={{minHeight:"80vh"}}>
        <LoginForm/>
    </div>
  )
}

export default LoginPage